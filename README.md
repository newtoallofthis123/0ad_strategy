# 0AG Field Manual

Pure static magazine-style shell for the 0 A.D. manuals.

## Structure

- `index.html` is the publication front door.
- `assets/site.css` styles the outer website shell.
- `manuals/` contains the individual manual volumes and the PDF.

## Cloudflare Pages

Use the repository root as the project output directory. No build command is required.
